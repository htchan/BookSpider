version: "3.7"

services:
    backend:
        build: 
            context: ./backend
            dockerfile: Dockerfile.backend
        volumes:
            - ./bin/database:/usr/src/app/database
            - ./bin/log:/log
            - /mnt/addition/download/Books:/usr/src/app/Books
        networks:
            - router
        profiles:
            - backend
            - web
        command: sh -c "./backend > /log/backend.log"

    frontend:
        build:
            context: ./frontend
            dockerfile: Dockerfile.flutter
        volumes:
            - frontend_volume:/build/web
            - ./frontend/src:/usr/src/app
        networks:
            - router
        profiles:
            - frontend
            - web
        command: sh -c "flutter --version ; flutter pub get ; flutter build web"
    
    backend_test:
        build:
            context: ./backend
            dockerfile: Dockerfile.test
        profiles:
            - test
        command: "go test github.com/htchan/BookSpider/helper \
		    github.com/htchan/BookSpider/model \
		    github.com/htchan/BookSpider/public -v"

volumes:
    frontend_volume:
        name: novel_frontend_volume

networks:
    router:
        driver: bridge
        name: router