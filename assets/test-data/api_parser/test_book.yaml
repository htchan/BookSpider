info:
  data:
    - "(?P<Title>title.*?) "
    - "(?P<Writer>writer.*?) "
    - "(?P<Type>type.*?) "
    - "date-(?P<LastUpdate>.*?) "
    - "(?P<LastChapter>chapter.*?) "
  items: "url-(?P<ChapterUrl>.*?) title-(?P<ChapterTitle>.*?) "

chapter_content:
  data:
    - "chapter-content-(?P<ChapterContent>.*)"